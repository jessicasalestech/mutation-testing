{
  "name": "mutation-testing-graos",
  "version": "1.0.0",
  "description": "QA Portfolio — Mutation Testing with Stryker + Jest. Demonstrates that code coverage ≠ test quality, applied to service fee business rules for grain reception.",
  "author": "Jessica Sales",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:mutation": "stryker run",
    "test:mutation:solution": "cross-env SOLUTION=1 stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.0",
    "@stryker-mutator/jest-runner": "^8.7.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0"
  }
}